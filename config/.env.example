# Kafka 설정
KAFKA_BOOTSTRAP_SERVERS=
KAFKA_SECURITY_PROTOCOL=
KAFKA_MAX_BATCH_SIZE=
KAFKA_MAX_REQUEST_SIZE=
KAFKA_CONSUMER_GROUP_ID=
KAFKA_COMMAND_TOPIC=
KAFKA_EVENT_TOPIC=
KAFKA_AUTO_OFFSET_RESET=

# Redis 설정
REDIS_HOST=
REDIS_PORT=
REDIS_DB=
REDIS_PASSWORD=
REDIS_SSL=
REDIS_CONNECTION_TIMEOUT=
REDIS_DEFAULT_TTL=

# 로깅 설정
LOG_LEVEL=INFO

# WebSocket/Connection 설정
# 하트비트 전송 간격(초)
WS_HEARTBEAT_INTERVAL=30
# PONG 대기 타임아웃(초)
WS_HEARTBEAT_TIMEOUT=10
# 하트비트 연속 실패 허용 횟수
WS_HEARTBEAT_FAIL_LIMIT=3
# 수신 정지 워치독 타임아웃(초)
WS_RECEIVE_IDLE_TIMEOUT=120